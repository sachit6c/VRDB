{
  "name": "vrdb",
  "version": "2.1.0",
  "description": "A movie & TV backlog app for two — Tinder-style swiping with a shared matched list",
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "test": "node --test --experimental-test-module-mocks",
    "db:migrate": "node scripts/db-migrate.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "jsdom": "^29.1.1",
    "postgres": "^3.4.4"
  }
}
